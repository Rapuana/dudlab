# Ansible vault password file — never commit this
.vault_pass

# Ansible retry files
*.retry

# Local overrides
*.local.yml

# Downloaded Ansible collections — install with: make install-deps
.ansible/
